/**
 * @jsx jsx
 */
import { jsx, css } from "@emotion/core";

const C1 = () => {
  /**
   * Javascript를 HTML에 삽입하는 일차적인 방법은 <script> 요소를 통하는 것
   * <script> 태그는 6가지 속성을 가진다.
   * async, defer, src, charset, language, type
   *
   *
   */

  return <div></div>;
};

export default C1;
